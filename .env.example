# Bingo Rush game server base URL used by the React app (src/lib/api.ts)
# Public (Traefik+Cloudflare). If Cloudflare challenges API/WS calls, relax the
# challenge for this host or point at the origin.
VITE_API_URL=https://bingo.jfmcss.com
